[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for History API routes (e.g. /schedule)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
